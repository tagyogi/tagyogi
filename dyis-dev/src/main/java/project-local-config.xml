<?xml version="1.0" encoding="UTF-8"?>
<application-definition>
	<active>local</active>
  <!--  <dbType>oracle</dbType>-->
    <dbType>h2</dbType>
    <domain>http://localhost:9999/</domain>

    <devUser>
        <mac>CA-FE-E9-21-08-99</mac> <!-- bsg -->
        
        <id>psj</id> <!-- bsg -->

        <name>박상준</name> <!-- bsg -->
        
    </devUser>

   <devInit>
        <h2>
            <devDbMas>
                <table>
                    CREATE TABLE DEV_DB_MAS (
                      TBL_SN   INT            NOT NULL,
                      TBL_NM   VARCHAR(4000)  NOT NULL,
                      TBL_CMT  VARCHAR(1000),
                      USE_AT   CHAR(1),
                      PRIMARY KEY (TBL_SN)
                    )
                </table>
                <index>
                    CREATE UNIQUE INDEX IDX_DEV_DB_MAS_PK ON DEV_DB_MAS(TBL_SN)
                </index>
            </devDbMas>
            <devDbDtl>
                <table>
                    CREATE TABLE DEV_DB_DTL (
                          TBL_SN    INT            NOT NULL,
                          COL_SN    INT            NOT NULL,
                          COL_NM    VARCHAR(50),
                          COL_CMT   VARCHAR(1000),
                          COL_TYPE  VARCHAR(1000),
                          COL_LEN   INT,
                          COL_PRE   INT,
                          PK_AT     CHAR(1)        DEFAULT '0',
                          NULL_AT   CHAR(1)        DEFAULT '0',
                          DEF_VAL   VARCHAR(4000),
                          RMK       VARCHAR(4000),
                          PRIMARY KEY (TBL_SN, COL_SN)
                        )
                </table>
                <index>
                    CREATE UNIQUE INDEX IDX_DEV_DB_DTL_PK ON DEV_DB_DTL(TBL_SN, COL_SN)
                </index>
            </devDbDtl>
        </h2>
    </devInit>


</application-definition>